# Base URL of the Phase 8 FastAPI backend (mrs.api.main:app). Defaults to
# http://localhost:8000 in src/lib/api.ts if unset -- override only if you run
# uvicorn on a different host/port.
# VITE_API_BASE_URL=http://localhost:8000
